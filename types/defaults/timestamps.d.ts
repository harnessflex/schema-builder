export = timestamps;
/**
 * Create created_at and updated_at columns.
 *
 * @param {Boolean} currentTimeStamp
 * @returns {object}
 */
declare function timestamps(currentTimeStamp?: boolean): object;
